<ellipse
    style="fill:none;stroke:#000000;stroke-width:1"
    id="geometry{{ geometry.id }}"
    cx="{{ blueprint.left+geometry.left+(geometry.width/2) }}"
    cy="{{ blueprint.top+geometry.top+(geometry.height/2) }}"
    rx="{{ geometry.width/2 }}"
    ry="{{ geometry.height/2 }}" />