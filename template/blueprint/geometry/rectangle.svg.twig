<rect
    style="fill:none;stroke:#000000;stroke-width:1"
    width="{{ geometry.width }}"
    height="{{ geometry.height }}"
    x="{{ blueprint.left+geometry.left }}"
    y="{{ blueprint.top+geometry.top }}" />